export default function LocaleLayout({ children }: { children: React.ReactNode }) {
  // Locale-specific layout can be added here if needed. RootLayout already provides i18n provider.
  return children;
}

