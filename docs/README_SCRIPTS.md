# ToolScout AI - Windows 启动脚本包

## 📦 包含内容

本次为您创建了完整的 Windows 启动脚本套件，让您可以轻松管理前后端服务。

---

## 🎯 创建的文件

### 1. 启动脚本（位于 `docs/` 目录）

| 文件名 | 功能 | 快速访问 |
|--------|------|---------|
| `start-all.bat` | 一键启动前端+后端 | [查看](start-all.bat) |
| `start-backend.bat` | 只启动后端服务 | [查看](start-backend.bat) |
| `start-frontend.bat` | 只启动前端服务 | [查看](start-frontend.bat) |
| `stop-all.bat` | 停止所有服务 | [查看](stop-all.bat) |
| `check-environment.bat` | 🆕 环境检查工具 | [查看](check-environment.bat) |
| `install-dependencies.bat` | 🆕 依赖快速安装 | [查看](install-dependencies.bat) |

### 2. 文档文件

| 文件名 | 说明 |
|--------|------|
| `Windows启动脚本说明.md` | 详细使用说明、常见问题解答 |
| `QUICK_START.md` | 更新了 Windows 快速启动部分 |

---

## 🚀 快速开始（3 步搞定）

### 第 0 步：环境检查（推荐）

```cmd
# 先检查环境是否完整
docs\check-environment.bat
```

### 第 1 步：确保环境已安装

```cmd
# 检查 Python
python --version
# 应该显示: Python 3.11.x 或更高

# 检查 Node.js
node -v
# 应该显示: v18.x.x 或更高
```

**如果依赖未安装，可以运行：**
```cmd
docs\install-dependencies.bat
```

### 第 2 步：配置数据库（仅首次）

编辑 `backend/.env` 文件，确保数据库连接正确：

```env
DATABASE_URL=postgresql://postgres:2qignzJ7BiyoxeOs@db.bjnxwxqyroxerjlotzjz.supabase.co:5432/postgres
```

### 第 3 步：启动服务

```cmd
# 双击运行或命令行执行
docs\start-all.bat
```

就这么简单！🎉

---

## 🎨 脚本特性

### ✨ 智能检测
- ✅ 自动检测虚拟环境
- ✅ 自动检测依赖安装状态
- ✅ 自动检测配置文件

### 🔧 自动修复
- ✅ 缺少依赖？自动安装
- ✅ 缺少配置？自动创建
- ✅ 缺少虚拟环境？提示创建

### 💡 友好提示
- ✅ UTF-8 中文支持
- ✅ 彩色输出（使用 ANSI 代码）
- ✅ 清晰的状态显示
- ✅ 详细的错误提示

---

## 📖 使用场景

### 场景 1：开始一天的工作

```cmd
# 一键启动所有服务
docs\start-all.bat

# 访问 http://localhost:3000 开始开发
```

### 场景 2：只需要修改前端

```cmd
# 只启动前端，节省资源
docs\start-frontend.bat
```

### 场景 3：只需要测试 API

```cmd
# 只启动后端
docs\start-backend.bat

# 访问 http://localhost:8000/docs 测试 API
```

### 场景 4：结束工作

```cmd
# 停止所有服务，释放端口
docs\stop-all.bat
```

---

## 🔍 脚本执行流程

### start-backend.bat 流程图

```
开始
  ↓
切换到 backend 目录
  ↓
检查虚拟环境 (venv)
  ├─ 存在 → 激活虚拟环境
  └─ 不存在 → 提示创建
  ↓
检查 FastAPI 是否安装
  ├─ 已安装 → 继续
  └─ 未安装 → 自动 pip install
  ↓
检查 .env 配置文件
  ├─ 存在 → 继续
  └─ 不存在 → 警告并暂停
  ↓
启动 Uvicorn 服务器
  ↓
显示服务地址
  ↓
等待用户按键
```

### start-frontend.bat 流程图

```
开始
  ↓
切换到 frontend 目录
  ↓
检查 Node.js 安装
  ├─ 已安装 → 显示版本
  └─ 未安装 → 错误退出
  ↓
检查 node_modules
  ├─ 存在 → 继续
  └─ 不存在 → 自动 npm install
  ↓
检查 .env.local 配置
  ├─ 存在 → 继续
  └─ 不存在 → 自动创建默认配置
  ↓
启动 Next.js 开发服务器
  ↓
显示服务地址
  ↓
等待用户按键
```

### start-all.bat 流程图

```
开始
  ↓
打开新窗口：后端服务
  ↓
等待 2 秒
  ↓
打开新窗口：前端服务
  ↓
显示所有服务地址
  ↓
等待用户按键
```

---

## 🛠️ 技术细节

### 脚本技术要点

#### 1. UTF-8 编码支持
```batch
@echo off
chcp 65001 >nul
```
- 设置控制台为 UTF-8 编码
- 支持中文显示

#### 2. 路径处理
```batch
cd /d "%~dp0..\backend"
```
- `%~dp0`：获取脚本所在目录
- `/d`：允许跨盘符切换
- `..`：返回上级目录

#### 3. 条件检测
```batch
if exist "venv\Scripts\activate.bat" (
    echo ✅ 检测到虚拟环境
) else (
    echo ⚠️  未检测到虚拟环境
)
```

#### 4. 进程管理
```batch
# 查找占用端口的进程
for /f "tokens=5" %%a in ('netstat -ano ^| findstr :8000 ^| findstr LISTENING') do (
    taskkill /F /PID %%a
)
```

---

## 🐛 故障排除

### 常见错误和解决方案

#### 错误 1：脚本闪退

**现象：** 双击后窗口一闪而过

**原因：**
- 路径不正确
- Python/Node.js 未安装

**解决：**
```cmd
# 在命令行运行，查看错误
cmd
cd toolscout-ai
docs\start-backend.bat
```

#### 错误 2：uvicorn 命令未找到

**现象：**
```
'uvicorn' 不是内部或外部命令
```

**解决：**
```cmd
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
pip install bcrypt pyjwt email-validator
```

#### 错误 3：端口被占用

**现象：**
```
Error: listen EADDRINUSE :::8000
```

**解决：**
```cmd
# 使用停止脚本
docs\stop-all.bat

# 或手动查找进程
netstat -ano | findstr :8000
taskkill /F /PID <进程ID>
```

#### 错误 4：数据库连接失败

**现象：**
```
sqlalchemy.exc.OperationalError
```

**解决：**
1. 检查 `backend/.env` 中的 `DATABASE_URL`
2. 确认 Supabase 项目状态
3. 测试网络连接

---

## 📊 服务端口汇总

| 服务 | 端口 | 地址 | 说明 |
|-----|------|------|------|
| 后端 API | 8000 | http://localhost:8000 | FastAPI 服务 |
| API 文档 | 8000 | http://localhost:8000/docs | Swagger UI |
| 健康检查 | 8000 | http://localhost:8000/health | 健康状态 |
| 前端应用 | 3000 | http://localhost:3000 | Next.js 应用 |

---

## 📁 项目结构

```
toolscout-ai/
├── docs/                          # 文档目录
│   ├── start-all.bat             # ⭐ 启动所有服务
│   ├── start-backend.bat         # ⭐ 启动后端
│   ├── start-frontend.bat        # ⭐ 启动前端
│   ├── stop-all.bat              # ⭐ 停止所有服务
│   ├── Windows启动脚本说明.md     # 详细使用说明
│   └── QUICK_START.md            # 快速开始指南
├── backend/                       # 后端目录
│   ├── app/                      # 应用代码
│   ├── venv/                     # 虚拟环境（需创建）
│   ├── .env                      # 环境配置（需配置）
│   └── requirements.txt          # Python 依赖
└── frontend/                      # 前端目录
    ├── src/                      # 源代码
    ├── node_modules/             # npm 依赖（自动安装）
    ├── .env.local                # 环境配置（自动创建）
    └── package.json              # npm 配置
```

---

## 🎓 学习资源

### 后端开发
- FastAPI 官方文档: https://fastapi.tiangolo.com/zh/
- SQLAlchemy 文档: https://docs.sqlalchemy.org/
- Pydantic 文档: https://docs.pydantic.dev/

### 前端开发
- Next.js 官方文档: https://nextjs.org/docs
- React 官方文档: https://react.dev/
- Tailwind CSS: https://tailwindcss.com/docs

### 数据库
- Supabase 文档: https://supabase.com/docs
- PostgreSQL 文档: https://www.postgresql.org/docs/

---

## 🤝 贡献指南

如果您想改进这些脚本：

1. 修改 `docs/` 目录下的 `.bat` 文件
2. 测试您的修改
3. 更新文档说明
4. 提交 Pull Request

---

## 📞 获取帮助

遇到问题？

1. 📖 查看 [Windows启动脚本说明.md](./Windows启动脚本说明.md)
2. 📖 查看 [QUICK_START.md](./QUICK_START.md)
3. 🐛 提交 Issue
4. 💬 联系开发团队

---

## ✅ 验证清单

在开始开发前，请确保：

- [ ] Python 3.11+ 已安装
- [ ] Node.js 18+ 已安装
- [ ] `backend/.env` 已配置数据库连接
- [ ] 虚拟环境已创建（或让脚本自动提示）
- [ ] 双击 `docs\start-all.bat` 能成功启动

---

## 🎉 完成！

现在您可以：

1. **双击 `start-all.bat`** 启动所有服务
2. **访问 http://localhost:3000** 查看前端
3. **访问 http://localhost:8000/docs** 测试 API
4. **开始愉快的开发！** 🚀

---

**祝您开发顺利！如有问题，随时查看文档。** ✨
