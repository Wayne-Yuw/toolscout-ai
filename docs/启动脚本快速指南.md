# 🚀 ToolScout AI - 启动脚本快速指南

## 📋 首次使用流程

```
┌─────────────────────────────────────────────────────────────┐
│                     首次使用流程图                           │
└─────────────────────────────────────────────────────────────┘

第 1 步: 检查环境
┌──────────────────────┐
│ check-environment.bat │  ← 运行环境检查
└──────────────────────┘
          ↓
    环境完整？
          ├─ 是 → 跳到第 3 步
          └─ 否 ↓

第 2 步: 安装依赖
┌───────────────────────┐
│ install-dependencies.bat │  ← 自动安装所有依赖
└───────────────────────┘
          ↓

第 3 步: 配置数据库
┌──────────────────────┐
│ 编辑 backend/.env     │  ← 配置 DATABASE_URL
└──────────────────────┘
          ↓

第 4 步: 启动服务
┌──────────────────────┐
│   start-all.bat       │  ← 一键启动前后端
└──────────────────────┘
          ↓

第 5 步: 访问应用
┌──────────────────────┐
│ http://localhost:3000 │  ← 前端界面
│ http://localhost:8000 │  ← 后端 API
└──────────────────────┘
```

---

## 🛠️ 日常开发流程

```
┌─────────────────────────────────────────────────────────────┐
│                     日常开发流程图                           │
└─────────────────────────────────────────────────────────────┘

开始工作
    ↓
┌──────────────────┐
│  start-all.bat   │  ← 一键启动所有服务
└──────────────────┘
    ↓
  开发中...
    ↓
结束工作
    ↓
┌──────────────────┐
│  stop-all.bat    │  ← 停止所有服务
└──────────────────┘
```

---

## 🎯 脚本选择指南

### 我应该运行哪个脚本？

```
┌──────────────────────────────────────────────────────────────┐
│                    场景 → 脚本对照表                          │
└──────────────────────────────────────────────────────────────┘

场景 1: 第一次使用
    ↓
    check-environment.bat        # 检查环境
    ↓
    install-dependencies.bat     # 安装依赖
    ↓
    配置 backend/.env            # 设置数据库
    ↓
    start-all.bat                # 启动服务

场景 2: 开始一天的工作
    ↓
    start-all.bat                # 启动所有服务

场景 3: 只需要修改前端
    ↓
    start-frontend.bat           # 只启动前端

场景 4: 只需要测试 API
    ↓
    start-backend.bat            # 只启动后端

场景 5: 遇到依赖问题
    ↓
    install-dependencies.bat     # 重新安装依赖

场景 6: 服务无响应
    ↓
    stop-all.bat                 # 强制停止
    ↓
    start-all.bat                # 重新启动

场景 7: 环境问题排查
    ↓
    check-environment.bat        # 诊断环境
```

---

## 📊 脚本功能对比

| 脚本名称 | 启动前端 | 启动后端 | 安装依赖 | 检查环境 | 停止服务 |
|---------|---------|---------|---------|---------|---------|
| start-all.bat | ✅ | ✅ | ❌ | ❌ | ❌ |
| start-backend.bat | ❌ | ✅ | 自动* | 部分 | ❌ |
| start-frontend.bat | ✅ | ❌ | 自动* | 部分 | ❌ |
| stop-all.bat | ✅ | ✅ | ❌ | ❌ | ✅ |
| check-environment.bat | ❌ | ❌ | ❌ | ✅ | ❌ |
| install-dependencies.bat | ❌ | ❌ | ✅ | ❌ | ❌ |

*自动：如果检测到缺失会自动安装

---

## ⚡ 快捷命令

### 所有脚本都在 `docs\` 目录下

```cmd
# 从项目根目录运行
cd toolscout-ai

# 环境检查
docs\check-environment.bat

# 安装依赖
docs\install-dependencies.bat

# 启动服务
docs\start-all.bat          # 启动所有
docs\start-backend.bat      # 只启动后端
docs\start-frontend.bat     # 只启动前端

# 停止服务
docs\stop-all.bat
```

---

## 🔍 故障排查流程

```
┌─────────────────────────────────────────────────────────────┐
│                     故障排查流程图                           │
└─────────────────────────────────────────────────────────────┘

遇到问题
    ↓
运行环境检查
┌───────────────────────┐
│ check-environment.bat │
└───────────────────────┘
    ↓
检查结果
    ├─ 环境不完整 → 安装缺失的软件 (Python/Node.js)
    ├─ 依赖缺失   → install-dependencies.bat
    ├─ 配置问题   → 检查 backend/.env
    ├─ 端口占用   → stop-all.bat
    └─ 全部正常   ↓

查看详细错误
┌──────────────────────┐
│ 直接在命令行运行脚本  │
│ 观察错误信息          │
└──────────────────────┘
    ↓
查看文档
┌──────────────────────┐
│ Windows启动脚本说明.md │
│ 常见问题解答          │
└──────────────────────┘
```

---

## 💡 使用技巧

### 1. 双击 vs 命令行

**双击运行：**
- ✅ 最简单
- ❌ 窗口可能闪退看不到错误

**命令行运行：**
- ✅ 可以看到完整输出
- ✅ 可以复制错误信息
- ✅ 便于排查问题

### 2. 虚拟环境（推荐）

```cmd
# 创建虚拟环境
cd backend
python -m venv venv

# 脚本会自动检测并使用虚拟环境
```

### 3. 查看服务状态

```cmd
# 检查端口占用
netstat -ano | findstr ":8000"   # 后端
netstat -ano | findstr ":3000"   # 前端

# 访问健康检查
curl http://localhost:8000/health
```

---

## 📞 获取帮助

### 文档资源

1. **快速开始**: [QUICK_START.md](./QUICK_START.md)
2. **详细说明**: [Windows启动脚本说明.md](./Windows启动脚本说明.md)
3. **脚本总览**: [README_SCRIPTS.md](./README_SCRIPTS.md)

### 常见问题速查

| 问题 | 解决方案 | 文档位置 |
|-----|---------|---------|
| Python 未安装 | 安装 Python 3.11+ | QUICK_START.md |
| Node.js 未安装 | 安装 Node.js 18+ | QUICK_START.md |
| 依赖安装失败 | 运行 install-dependencies.bat | 本文档 |
| 端口被占用 | 运行 stop-all.bat | Windows启动脚本说明.md |
| 数据库连接失败 | 检查 backend/.env | Windows启动脚本说明.md |

---

## ✅ 验证清单

使用前确认：

- [ ] Python 3.11+ 已安装 (`python --version`)
- [ ] Node.js 18+ 已安装 (`node -v`)
- [ ] 后端依赖已安装（或让脚本自动安装）
- [ ] 前端依赖已安装（或让脚本自动安装）
- [ ] `backend/.env` 已配置数据库连接
- [ ] 端口 8000 和 3000 未被占用

---

**准备好了吗？运行 `docs\start-all.bat` 开始吧！** 🚀
